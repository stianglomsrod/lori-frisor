---
import Logo from "./Logo.astro";
import LanguageToggle from "./LanguageToggle.astro";
import NewTab from "./NewTab.astro";
import { getSettings } from "@lib/content";
import { resolveLocale, localeHome, type Locale } from "@i18n/config";
import { useTranslations } from "@i18n/ui";

interface Props {
  /** Mål for språkvelgeren på undersider (f.eks. personvern ↔ privacy). */
  langPaths?: Partial<Record<Locale, string>>;
}
const { langPaths } = Astro.props;

const locale = resolveLocale(Astro.currentLocale);
const t = useTranslations(locale);
const { booking } = await getSettings();
// Absolutte ankere («/#tjenester») slik at menyen også virker fra undersider
// (personvern, 404). På forsiden oppfører de seg som vanlige ankerhopp.
const home = localeHome(locale);
---

<header class="site-header">
  <div class="container site-header__inner">
    <a class="site-header__brand" href={`${home}#hovedinnhold`} aria-label={t.header.toTop}>
      <Logo />
    </a>

    <div class="site-header__actions">
      <div class="site-header__lang">
        <LanguageToggle paths={langPaths} />
      </div>
      <a
        class="btn btn--primary site-header__cta"
        href={booking.url}
        target="_blank"
        rel="noopener"
      >
        {t.services.book}<NewTab />
      </a>
      <nav class="site-nav" aria-label={t.header.menuAria}>
        <button
          class="nav-toggle"
          type="button"
          aria-expanded="false"
          aria-controls="hovedmeny"
          aria-label={t.header.menu}
        >
          <span class="nav-toggle__bars" aria-hidden="true"></span>
          <span class="nav-toggle__label">{t.header.menu}</span>
        </button>

        <div class="site-nav__panel" id="hovedmeny" data-nav-panel>
          <ul class="site-nav__list">
            {
              t.nav.map((link) => (
                <li>
                  <a href={`${home}${link.href}`}>{link.label}</a>
                </li>
              ))
            }
          </ul>
          <div class="site-nav__lang">
            <LanguageToggle paths={langPaths} />
          </div>
          <a class="btn btn--primary site-nav__cta" href={booking.url} target="_blank" rel="noopener">
            {t.cta.book}<NewTab />
          </a>
        </div>
      </nav>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: color-mix(in srgb, var(--color-cream) 88%, transparent);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--color-line);
  }
  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    min-height: 4.5rem;
  }
  .site-header__brand {
    text-decoration: none;
    border-radius: 8px;
  }

  .site-header__actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  /* Kompakt «Bestill time» synlig i headeren på mobil, slik at primær-CTA
     alltid er innen rekkevidde uten å åpne menyen. Skjules på desktop, der
     panel-CTA-en tar over. */
  .site-header__cta {
    min-height: 2.5rem;
    padding: 0.45rem 1rem;
    font-size: 0.92rem;
    white-space: nowrap;
  }
  /* Svært smale skjermer: stram gap og vis menyknappen som ikon. */
  @media (max-width: 22.5rem) {
    .site-header__actions {
      gap: 0.4rem;
    }
    .nav-toggle__label {
      display: none;
    }
  }

  /* Språkvelger vises i menypanelet på mobil, inline på desktop */
  .site-header__lang {
    display: none;
  }
  .site-nav__lang {
    display: flex;
    justify-content: center;
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid var(--color-line);
  }

  .nav-toggle {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    min-height: 2.75rem;
    padding: 0.4rem 0.9rem;
    background: var(--color-white);
    border: 1px solid var(--color-line);
    border-radius: 999px;
    font: inherit;
    font-weight: 600;
    color: var(--color-ink);
    cursor: pointer;
  }
  .nav-toggle__bars,
  .nav-toggle__bars::before,
  .nav-toggle__bars::after {
    content: "";
    display: block;
    width: 20px;
    height: 2px;
    background: var(--color-ink);
    border-radius: 2px;
    transition: transform 0.2s ease, opacity 0.2s ease;
  }
  .nav-toggle__bars {
    position: relative;
  }
  .nav-toggle__bars::before {
    position: absolute;
    top: -6px;
  }
  .nav-toggle__bars::after {
    position: absolute;
    top: 6px;
  }
  .nav-toggle[aria-expanded="true"] .nav-toggle__bars {
    background: transparent;
  }
  .nav-toggle[aria-expanded="true"] .nav-toggle__bars::before {
    top: 0;
    transform: rotate(45deg);
  }
  .nav-toggle[aria-expanded="true"] .nav-toggle__bars::after {
    top: 0;
    transform: rotate(-45deg);
  }

  .site-nav__panel {
    display: none;
  }
  .site-nav__panel.is-open {
    display: block;
    position: absolute;
    left: 0;
    right: 0;
    top: 100%;
    background: var(--color-cream);
    border-bottom: 1px solid var(--color-line);
    box-shadow: var(--shadow-md);
    padding: 1rem var(--gutter) 1.5rem;
  }
  .site-nav__list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .site-nav__list a {
    display: block;
    padding: 0.85rem 0.5rem;
    font-weight: 600;
    color: var(--color-ink);
    text-decoration: none;
    border-radius: 8px;
    border-bottom: 1px solid var(--color-line);
  }
  .site-nav__list a:hover {
    color: var(--color-accent-dark);
  }
  .site-nav__cta {
    margin-top: 1rem;
    width: 100%;
  }

  /* Desktop: horisontal meny, ingen toggle */
  @media (min-width: 56rem) {
    .nav-toggle {
      display: none;
    }
    .site-header__cta {
      display: none;
    }
    .site-header__lang {
      display: inline-flex;
    }
    .site-nav__lang {
      display: none;
    }
    .site-nav {
      display: flex;
    }
    .site-nav__panel,
    .site-nav__panel.is-open {
      display: flex;
      align-items: center;
      gap: 1.75rem;
      position: static;
      padding: 0;
      background: none;
      border: 0;
      box-shadow: none;
    }
    .site-nav__list {
      flex-direction: row;
      gap: 1.5rem;
    }
    .site-nav__list a {
      padding: 0.4rem 0;
      border-bottom: 2px solid transparent;
    }
    .site-nav__list a:hover {
      border-bottom-color: var(--color-accent);
    }
    .site-nav__cta {
      margin-top: 0;
      width: auto;
    }
  }
</style>

<script>
  const toggle = document.querySelector<HTMLButtonElement>(".nav-toggle");
  const panel = document.querySelector<HTMLElement>("[data-nav-panel]");

  if (toggle && panel) {
    const setOpen = (open: boolean) => {
      toggle.setAttribute("aria-expanded", String(open));
      panel.classList.toggle("is-open", open);
    };

    toggle.addEventListener("click", () => {
      setOpen(toggle.getAttribute("aria-expanded") !== "true");
    });

    // Lukk ved valg av lenke
    panel.querySelectorAll("a").forEach((link) =>
      link.addEventListener("click", () => setOpen(false))
    );

    // Lukk med Escape
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape" && toggle.getAttribute("aria-expanded") === "true") {
        setOpen(false);
        toggle.focus();
      }
    });
  }
</script>
